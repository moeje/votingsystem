require 'spec_helper'

describe "a user has access to Poll CRUD" do
  it "can add a new poll" do
    puts Factory.create(:poll)
  end
  it "can edit a poll"
  it "can delete a poll"
  it "can change a poll"
end